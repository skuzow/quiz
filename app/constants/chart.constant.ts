export enum Chart {
  COMPLETED = 'completed',
  PASSEDFAILED = 'passedfailed'
}

export enum ChartKey {
  DAY = 'day',
  MONTH = 'month',
  YEAR = 'year'
}

export enum ChartUnit {
  HOUR = 'hour',
  DAY = 'day',
  MONTH = 'month'
}
